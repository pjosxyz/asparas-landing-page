---
interface Props {
  variant: string;
  glow?: boolean;
}

const { variant,  glow = false } = Astro.props;
---

<button class="text-logotext text-xl border-2 border-violet-600 px-6 py-4 rounded-lg flex items-center justify-center">
  <slot />
</button>
